# MP3 Tag Checker - Python dependencies
#
# Normally you do NOT need to run anything by hand: run.bat checks Python,
# creates the virtual environment and installs everything below automatically.
#
# Manual installation (run in this folder, e.g. in cmd):
#   py -3 -m venv .venv
#   .venv\Scripts\python.exe -m pip install --upgrade pip
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# ...and start the app with:
#   .venv\Scripts\python.exe app.py
#
# (Lines starting with # are comments - pip ignores them.)

# Minimum versions: the first releases with official Python 3.13 support
# (verified with Python 3.13 / PySide6 6.11 / Pillow 12 / mutagen 1.48).
mutagen>=1.47
Pillow>=11.0
PySide6>=6.8
requests>=2.32
