# MP3 Tag Checker – Python-Projektdefinition.
#
# Projekt:     MP3 Tag Checker
# Modul:       pyproject.toml
# Version:     1.12.0
# Stand:       2026-08-18
# Abhaengig:   Python >= 3.10; mutagen>=1.47; Pillow>=11.0; PySide6>=6.8; requests>=2.32
# Bezug:       requirements.txt
# Lizenz:      MIT
# Upstream:    https://github.com/DarkKoNO/mp3-tag-checker (Jakub Konopasek)
# Erstellt mit: Cursor Grok 4.6
# Autor:       (FFHB) / RadioBBS
#
# Historie
# --------
# Version 1.12.0 – 2026-08-21 – Nachzug pyproject.toml (Software-Standard 1.1.0)
#
# Laufzeit-Abhaengigkeiten weiterhin in requirements.txt installieren:
#   python -m pip install -r requirements.txt

[project]
name = "mp3-tag-checker"
version = "1.12.0"
description = "GUI zum Pruefen und Reparieren von ID3-Tags."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "(FFHB) / RadioBBS" }]
dependencies = [
    "mutagen>=1.47",
    "Pillow>=11.0",
    "PySide6>=6.8",
    "requests>=2.32",
]
